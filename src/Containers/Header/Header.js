import React from "react";import "./Header.css";import SearchIcon from "@material-ui/icons/Search";import ShoppingBasketIcon from "@material-ui/icons/ShoppingBasket"import {Link} from "react-router-dom";import {useStateValue} from "../../StateProvider";import {auth} from "../../firebase";function Header(){    // eslint-disable-next-line    const [{basket,user},dispatch]= useStateValue();    const handleAuthentication = ()=>{        if (user){            auth.signOut()                .then(()=>{                    console.log('succesfully signed out.')                })        }    }    return(        <header className="header">            <Link to="/">                <img className="amazon-logo" src="http://pngimg.com/uploads/amazon/amazon_PNG11.png" alt="" />            </Link>            <div className="input-search-box">                <input className="inputSearch" type="search" />                <SearchIcon className="searchIcon"/>            </div>            <div className="header-nav">                <Link to={!user && "/login"}>                    <div onClick={handleAuthentication} className="header-option">                        <span className="header-option-lineOne">Hello {                            user?.email ? user.email: 'Guest'                        } ,</span>                        <span className="header-option-lineTwo">Sign {user?.email ? 'Out' : 'In' }</span>                    </div>                </Link>                <div className="header-option">                    <span className="header-option-lineOne">Returns</span>                    <span className="header-option-lineTwo">& Orders</span>                </div>                <div className="header-option">                    <span className="header-option-lineOne">Your</span>                    <span className="header-option-lineTwo">Prime</span>                </div>                <Link to="/checkout">                    <div className="header-shopping-basket">                        <ShoppingBasketIcon className="shopping-basket-icon"/>                        <span className="shopping-basket-count">                            {basket?.length}                        </span>                    </div>                </Link>            </div>        </header>    );}export default Header;