import React from "react";import "./BasketProduct.css";import {useStateValue} from "../../../StateProvider";function BasketProduct({uuid,id, title, image, rating, price}) {    // eslint-disable-next-line    const [{basket},dispatch]= useStateValue();    const removeFromBasket = ()=>{        dispatch({            type:"REMOVE_FROM_BASKET",            id:id,            uuid:uuid        })    }    return (        <div className="basketProduct">            <div className="basket-product-text">                <p className="basket-product-title">{title}</p>                <p className="basket-product-price">                    <small>$</small>                    <strong>{price}</strong>                </p>                <div className="basket-product-rating">                    {Array(rating)                        .fill()                        .map((_, i) => (                            <p>                                <span role="img" aria-label="rating start emoji">🌟</span>                            </p>                        ))}                </div>                <button onClick={removeFromBasket} className="basket-product-button">Remove from basket</button>            </div>            <div className="basket-product-image-div">                <img className="basket-product-image" src={image} alt=""/>            </div>        </div>    );}export default BasketProduct;